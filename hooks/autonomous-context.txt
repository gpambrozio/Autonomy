You should work as autonomously as possible on this session so there's no need to get into plan mode to create a plan and ask the user for approval.

If you deem that the work cannot be done without user input then you can ask questions. Use the `AskUserQuestion` for this. But only do this if it is absolutely necessary, otherwise just do the work without user input.
